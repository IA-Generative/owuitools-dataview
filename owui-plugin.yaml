name: data-query-owui
version: "1.0.0"

services:
  - name: data-query
    port: 8093

pipelines:
  files: []

tools:
  entries:
    - id: data_query
      source_file: openwebui/data_query_tool.py
      service_name: data-query
      service_port: 8093

model_tools: []
env_vars: []

k8s:
  namespace: miraiku
  custom_image: true
